<% content_for :css do %>
<%= javascript_include_tag "count_form" %>
<%= stylesheet_link_tag 'form' %>
<% end %>


<%= bootstrap_form_with(model: post, local: true) do |form| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>
      <ul>
      <% post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field" id="countable">
    <%= form.text_field :title, id: "input_text", label: "タイトル" %>
    <p><span id="counter">0文字</span> / <span id="limit" data-count="35">最大文字数</span></p>
  </div>
  
  <div class="field">
    <% if @post.image.present? %>
    <div class="attached_img_area">
    <%=  cl_image_tag @post.image.thumb, alt: "サムネイル" %>
    </div>
    <% end %>
    <%= form.hidden_field :image_cache %>
    <%= form.file_field :image, label: "添付画像を選択" %>
  </div>
  
  

  <div class="field" id="countable">
    <%= form.text_area :caption, id: "input_text", label: "説明文" %>
    <p><span id="counter">0文字</span> / <span id="limit" data-count="1000">最大文字数</span></p>
  </div>

  <div class="field">
    <p>この投稿と関連づける作成済みのタグを選んでください。<br>
    デフォルトでは未選択となっています。<br>
    すでにタグ付けがされている場合、先頭の空白行を選択することでタグの解除ができます。
    </p>
    <%= form.collection_select(:tag_id, current_user.tags, :id, :name, include_blank: true, label: "タグの設定") %>
  </div>

  <div class="actions">
    <%= form.submit "投稿する" %>
  </div>
<% end %>
