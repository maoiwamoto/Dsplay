<%= javascript_include_tag 'post_crop' %>
<%= javascript_include_tag 'post_crop_js' %>
<%= stylesheet_link_tag 'crop_style' %>

<div class="crop_html_container">
  
     
        <h3>Crop Avatar</h3>
  
  
        <div class="croparea_box">
        <%= image_tag @post.image_url(:thumb), id: "cropbox" %>
        </div>
        
        <h3>Preview</h3>
        <div class="preview_box">
          <%= image_tag @post.image.url(:thumb), id: "preview" %>
        </div>
        
        <hr>
       <%= form_for(@post) do |f| %>
          <% %w[x y w h].each do |attribute| %>
            <%= f.text_field "crop_#{attribute}" %>
              <% end %>
          <div class="form-actions"><%= f.submit %></div>
        <% end %>
        
        <%= link_to 'トリミングをスキップする', posts_path %>

</div>