<li class="cards post">
            <% if post.crop_w.nil? %>
            <%= cl_image_tag post.image.safe, alt: "サムネイル" %>
            <% else %>
            <%= cl_image_tag post.image.tiny, alt: "サムネイル" %>
            <% end %>
    <%= link_to post_path(post), "aria-label" => "#{post.user.prof.name}の#{post.title}へ" do %>
    <span class="show">詳細を見る</span>
    <% end %>
    </li>